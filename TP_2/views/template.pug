
doctype html
html(lang="en")
  head
    title= Title
    script(type='text/javascript').
      if (foo) bar(1 + 5)
  body
    h1 Test Server express
    h2= name
      table
        each val in tab
          tr
            td= val
    #container.col
    br